<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="kr.urimal365.dao.ContentsMapper">
	<select id="getBanner" resultType="Contents" parameterType="String">
		SELECT contents, open_yn FROM NARO_CONTENTS WHERE del_yn='N'  AND date_format(NOW(), '%Y-%m-%d %H:%i') 
		BETWEEN  open_start_datetime  AND open_end_datetime  
			AND gbn=#{gbn} ORDER BY idx DESC LIMIT 1;
	</select>
	<select id="getFooter" resultType="Contents" parameterType="String">
		SELECT contents, open_yn FROM NARO_CONTENTS WHERE del_yn='N'  
			AND gbn=#{gbn} ORDER BY idx DESC LIMIT 1;
	</select>
</mapper>